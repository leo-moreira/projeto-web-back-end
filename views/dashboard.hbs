<header>
  <h1>Dashboard de Fotos</h1>
  <p>Olá, {{userName}}! Veja suas fotos.</p>
  <a href="/logout" class="logout-button">Sair</a>
</header>

<main>
  <h2>Suas Fotos</h2>
  <div class="photo-grid">
    {{#if fotos.length}}
      {{#each fotos}}
        <div class="photo-card">
          <img src="{{this.storageUrl}}" alt="{{this.title}}" />
          <div class="photo-info">
            <h3>{{this.title}}</h3>
            <p>{{this.description}}</p>
          </div>
        </div>
      {{/each}}
    {{else}}
      <p>Você ainda não tem fotos. Que tal adicionar algumas?</p>
    {{/if}}
  </div>
</main>